fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
httpx>=0.27,<1.0
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
opencv-python-headless>=4.9,<5.0
numpy>=1.26,<3.0
Pillow>=10.2,<12.0
python-multipart>=0.0.9,<1.0
# pytesseract is a thin wrapper around the external `tesseract` binary, which
# is NOT installed by pip. Install it separately (e.g. `brew install tesseract`
# / `apt-get install tesseract-ocr`) to enable OCR-backed text metrics. If the
# binary is missing at runtime, the metric engine falls back to empty OCR data.
pytesseract>=0.3.10,<1.0
# google-genai is only actually called when LLM_PROVIDER=gemini and
# GEMINI_API_KEY is set (app.llm.gemini_provider); the default "mock"
# provider and the full test suite never make a network call.
google-genai>=1.0,<2.0
# torch + transformers back the real UIClip provider
# (app.uiclip.huggingface_provider), only actually loaded/used when
# UICLIP_PROVIDER=huggingface. Installed unconditionally, matching how
# google-genai is handled above. The default "mock" UIClip provider and the
# full test suite never download or run a real model. First-time model
# download requires internet access; the checkpoint is cached locally
# afterward by the standard Hugging Face cache.
torch>=2.2,<3.0
transformers>=4.40,<5.0
